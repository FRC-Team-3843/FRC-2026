# FRC-2026 - Protocol

This file defines the local workflow for this repository.

This repo must remain usable on its own, without requiring `C:\GitHub` to exist.
If broader coordination context is available, `C:\GitHub\.protocol.md` is the preferred top-level entry point, but it is optional.

<!-- @always -->
## Critical rules (FRC-2026)

- **Build with the WPILib JDK17.** System `java` is 8 — builds fail with a Java-version error if `JAVA_HOME` is unset. From the `2026Robot` dir: `JAVA_HOME="C:/Users/Public/wpilib/2026/jdk" ./gradlew build` (likewise `deploy` / `test` / `simulateJava`). Run from the project dir, not the repo root.
- **Read `STANDARDS.md` before any code change.** It is the team's single source of truth: strict command-based + dependency injection (RobotContainer is the only instantiation/binding site), 2026 motor APIs only (`SparkMax`/`SparkMaxConfig`, Phoenix6 control requests), **no Phoenix Pro features**, no warning suppression, no un-refactored FRC-2024/2025 patterns.
- Verify CAN IDs before deploying to hardware (swerve 11-22, mechanisms 30-39); CAN map + bindings live in `2026Robot\TODO-COMPETITION.md` and `NOTES.md` §0B.
- Note: `.project-context.md` is stale (2026-01-23 pre-season template); `README.md` / `NOTES.md` / `2026Robot\TODO-COMPETITION.md` are the live truth.
<!-- /@always -->

## Read Order

1. `.changelog.md`
2. `.project-context.md`
3. `.standards.md`
4. `.protocol.md`

## During Work

- Follow `.standards.md`
- Check `.changelog.md` before overlapping work
- Keep `.project-context.md` current when state changes

## Logging

Use `.changelog.md` with this format:

```text
### [YYYY-MM-DD HH:MM] AGENT_NAME [ACTION_TYPE]
- Description of changes
- Repo: FRC-2026
- Files modified: <paths from repo root>
- Notes: Important context for other agents
- Why: Why this change was made
- PENDING: (optional) Follow-up work
```

## Working Files

- Use `.scratch/` for temporary scripts, debug artifacts, and working files
- Use `.research/` for reference materials, external tools, and research
- Both directories are gitignored — do not commit their contents

## Incomplete Work

Track incomplete work in `.project-context.md`:

- `TODO` for actionable follow-up
- `Pending Decisions` for unresolved choices
